apiVersion: v1
kind: Service
metadata:
  name: {{ include "jeverson.fullname" . }}
  labels:
    {{- include "jeverson.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jeverson.selectorLabels" . | nindent 4 }}
